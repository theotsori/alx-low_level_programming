#include "main.h"

/**
 * mul - multipliers two integers
 * @a: intto be used
 * @b: same as a
 *
 * Return: 0
 */
int add(int a, int b)
{
	return (a + b);
}
int sub(int a, int b)
{
	return (a - b);
}
int mul(int a, int b)
{
	return (a * b);
}
int _div(int a, int b)
{
	return (a / b);
}
int mod(int a, int b)
{
	return (a % b);
}
